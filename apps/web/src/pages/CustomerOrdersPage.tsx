import { useEffect, useState } from 'react'; import { PackageSearch } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { Order } from '../features/products/product.types'; import { useAuthStore } from '../features/auth/auth.store'; import { apiRequest } from '../services/api';
export function CustomerOrdersPage() { const token = useAuthStore((state) => state.token)!; const [orders, setOrders] = useState<Order[] | null>(null); const [error, setError] = useState(''); useEffect(() => { apiRequest<Order[]>('/orders', {}, token).then(setOrders).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : 'Riwayat gagal dimuat.')); }, [token]); return <main className="container-shell py-12 sm:py-16"><p className="section-kicker">Riwayat pesanan</p><h1 className="section-title">Perjalanan tanamanmu.</h1>{error && <div role="alert" className="mt-7 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}{!orders && !error && <div className="skeleton mt-8 h-72 rounded-[2rem]" />}{orders?.length === 0 && <div className="panel mt-8 grid min-h-72 place-items-center p-8 text-center"><div><PackageSearch className="mx-auto text-leaf-600" /><h2 className="mt-4 font-display text-2xl font-semibold">Belum ada pesanan</h2><Link to="/products" className="button-base button-primary mt-6 px-5 py-3">Mulai belanja</Link></div></div>}<div className="mt-8 grid gap-4">{orders?.map((order) => <Link key={order.id} to={`/orders/${order.orderNumber}`} className="panel grid gap-4 p-5 transition hover:-translate-y-1 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><p className="font-display text-xl font-semibold text-forest-950">{order.orderNumber}</p><p className="mt-1 text-xs text-slate-500">{new Date(order.createdAt).toLocaleDateString('id-ID', { dateStyle: 'long' })} · {order.items.length} jenis produk</p></div><Status value={order.status} /><strong className="text-forest-900">{rupiah(order.totalAmount)}</strong></Link>)}</div></main>; }
export function Status({ value }: { value: Order['status'] }) { const color = value === 'COMPLETED' ? 'bg-green-100 text-green-800' : value === 'CANCELLED' ? 'bg-red-100 text-red-700' : value === 'CONFIRMED' ? 'bg-blue-100 text-blue-800' : 'bg-amber-100 text-amber-800'; return <span className={`badge ${color}`}>{value}</span>; }
function rupiah(value: number) { return new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(value); }
