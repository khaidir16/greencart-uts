{
  "buildCommand": "npm run db:generate --workspace @greencart/api && npm run db:migrate:deploy --workspace @greencart/api && npm run db:seed --workspace @greencart/api && npm run build --workspace @greencart/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm ci",
  "functions": {
    "api/index.ts": {
      "includeFiles": "node_modules/swagger-ui-dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
